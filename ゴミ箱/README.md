# ゴミ箱

削除候補ファイルの一時保管場所。`git mv` で移動しているため履歴は保持されます。

**実際の削除（このフォルダごと消す）は、ユーザーが明示的に指示したときのみ実行します。**

---

## 移動履歴

| 移動日 | 元のパス | 移動理由 |
|---|---|---|
| 2026-04-05 | `archive/scripts/` | 財務モデル生成Pythonスクリプト。現在はExcelファイルに移行済みのため不要 |
